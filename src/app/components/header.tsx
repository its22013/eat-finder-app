import style from "@/app/home.module.css"

const Header: React.FC = () => {
    return(
        <div className={style.header}>
          <h1 className={style.text01}>Eat Finder</h1>
          </div>
    )
}
export default Header;